<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DoppioPOS" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_brand</item>
        <item name="colorPrimaryDark">@color/primary_brand_dark</item>
        <item name="colorAccent">@color/accent_caramel</item>
        <item name="android:statusBarColor">@color/primary_brand_dark</item>
        <item name="android:navigationBarColor">@color/primary_brand_dark</item>
    </style>
</resources>
